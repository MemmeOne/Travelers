<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Mapper">
	<select id="reviewboard" resultType="dto">
		select B.*from(select rownum rn, A.*from(select*from board order by idgroup desc, step asc)A)B where rn between (1+(#{page}-1)*10) and (1+(#{page}-1)*10)+9
	</select>
	
	<select id="totalcontent" resultType="int">
		select count(*) from board
	</select>
	
	<select id="contentview" resultType="dto">
		select*from board where id=#{id}
	</select>
	
	<update id="uphit">
		update board set hit=hit+1 where id=#{id}
	</update>
	
	<delete id="delete">
		delete from board where id=#{id}
	</delete>
	
	<update id="modify">
		update board set title=#{title}, content=#{content} where id=#{id}
	</update>
	
	<insert id="contentreg">
		insert into board(id,name,title,content,hit,idgroup,indent) values(board_seq.nextval,#{name},#{title},#{content},0,board_seq.currval,0,0)
	</insert>
	
	<select id="comments" resultType="cdto">
		select A.*from(select*from comments order by numgroup asc, indent asc, num asc)A where id=#{id}
	</select>
	
	<insert id="comment">
		insert into comments(id,num,numgroup,indent,cname,comments) values(#{id},comments_seq.nextval,comments_seq.currval,0,#{cname},#{comments})
	</insert>
	
	<select id="search" resultType="dto">
		select C.*from(select rownum rn, B.*from(select A.*from(select*from board where ${searchtype} like '%${search}%')A  order by idgroup desc, step asc)B)C where rn between (1+(#{page}-1)*10) and (1+(#{page}-1)*10)+9
	</select>
	
	<select id="totalsearch" resultType="int">
		select count(*) from board where ${searchtype} like '%${search}%'
	</select>
	
	<select id="commentcount" resultType="cn">
		select id, (count(*))count from comments group by id
	</select>
	
	<insert id="reply">
		insert into comments(id,num,numgroup,indent,cname,comments) values(#{id},comments_seq.nextval,#{numgroup},1,#{cname},#{comments})
	</insert>
	
	<update id="commentmodify">
		update comments set comments=#{comments} where num=#{num}
	</update>
	
	<!-- 
	<select id="list" resultType="com.care.dto.TestDTO">
		select * from mybatis_test where num=#{num}
	</select>
	
	<insert id="savedata" parameterType="dto"> 
		insert into mybatis_test values(#{num},#{name},#{mydate})
	</insert>
	
	<update id="updatedata" parameterType="dto">
		update mybatis_test set name=#{name}, mydate=#{mydate} where num=#{num}
	</update>

	<delete id="delete" parameterType="String">
		delete from mybatis_test where num=#{num}
	</delete>
	 -->
	
</mapper>
